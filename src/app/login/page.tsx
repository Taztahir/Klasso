import { LoginPage } from '@/components/LoginPage';

export const metadata = {
    title: 'Log In — Klasso',
    description: 'Sign in to your Klasso account.',
};

export default function Login() {
    return <LoginPage />;
}
